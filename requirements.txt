# iGEM 2026 Rare Disease Pre-diagnosis Clue Finder — deployment dependencies
# Install: pip install -r requirements.txt

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Graph & inference
networkx>=3.0

# Vector search
faiss-cpu>=1.7.0
numpy>=1.24.0

# Cloud API clients (DeepSeek NER + SiliconFlow embeddings)
openai>=1.0.0

# Layperson dict generation (Phase 9 — standalone script)
requests>=2.28.0
